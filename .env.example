# EMMA Build Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# Android Build Configuration
# ============================================================================

# Enable production crypto with liboqs (ON/OFF)
PRODUCTION_CRYPTO=ON

# App Customization
CI_APP_TITLE=EMMA
CI_APP_FILENAME=EMMA
CI_PACKAGE_ID=im.molly.app

# Build Variants (regex pattern)
# Options: prod(Gms|Foss), prodGms, prodFoss, staging(Gms|Foss)
CI_BUILD_VARIANTS=prod(Gms|Foss)

# Internal Testing Extensions
CI_FORCE_INTERNAL_USER_FLAG=false

# Google Maps API Key (optional)
# CI_MAPS_API_KEY=your_api_key_here

# ============================================================================
# APK Signing Configuration (optional)
# ============================================================================

# Path to keystore file (relative to project root)
# CI_KEYSTORE_PATH=./my-release-key.jks

# Keystore password
# CI_KEYSTORE_PASSWORD=your_keystore_password

# Key alias
# CI_KEYSTORE_ALIAS=my-alias

# ============================================================================
# Gradle Configuration
# ============================================================================

# Gradle JVM options
GRADLE_OPTS=-Dorg.gradle.daemon=false -Dorg.gradle.parallel=true -Dorg.gradle.workers.max=4

# ============================================================================
# Python Translation Server Configuration
# ============================================================================

# Server port
EMMA_SERVER_PORT=5353

# Server host (0.0.0.0 for Docker, localhost for local)
EMMA_SERVER_HOST=0.0.0.0

# ============================================================================
# Development Options
# ============================================================================

# Docker build cache (use --no-cache to disable)
# DOCKER_BUILDKIT=1
# COMPOSE_DOCKER_CLI_BUILD=1
